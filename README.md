# Competent — Nápověda pro studenty

MkDocs Material site pro studentský portál Competent.

## Vývoj

```
cd docs-studenti
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
mkdocs serve
```

Sister site pro administrátory: `../docs/`.
